---
title: 'Get Delta Notification'
openapi: 'GET /json-delta/notify'
---

Long-polling endpoint that returns current session and serial. Blocks until dataset changes if parameters match current values.

**Use Cases:**
- Real-time update notifications
- Efficient polling mechanism
- Event-driven validation systems
- Minimize unnecessary requests
